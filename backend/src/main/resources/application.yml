server:
  port: 8080

spring:
  config:
    import: optional:file:.env[.properties],optional:file:./backend/.env[.properties]
  profiles:
    active: dev

springdoc:
  swagger-ui:
    path: /swagger-ui.html
